# Database Connection (PostgreSQL or SQLite)
# Defaults to SQLite for instant out-of-the-box local running
DATABASE_URL=sqlite:///./academia_industry.db
# For PostgreSQL production:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/academia_industry_db

# Security & JWT Token Configuration
JWT_SECRET_KEY=sih-ai-academia-industry-connect-super-secret-jwt-key-2026
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Optional Google Gemini AI / LLM Configuration
# The platform functions with complete local ML (TF-IDF & Cosine Similarity) even without an API key!
AI_PROVIDER=local
GEMINI_API_KEY=
VITE_API_URL=https://skillbridge-ai-1uy8.onrender.com
